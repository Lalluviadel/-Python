ДЗ 06
01.03.21 - 03.03.21

1. Если честно, не поняла примечание 2:
"- Вы не знате заранее насколько идентичен шаблон строк файла. Попробуйте оценить это."

Возможно, требуется проверка, те ли значения попадают в мои переменные remote_addr, request_type, requested_resource?

Так как это требование поняла не четко и оно не указано явно, а фигурирует формулировка "попробуйте", я не стала включать это в код сдаваемой работы. Но для себя попробовала.
Например, для remote_addr так:
	if (num.isdigit for num in remote_addr.split('.')):
	перед yield.
Т.е. если в remote_addr не только цифры, значит, с шаблоном уже что-то не то и далее распарсить такой файл логов программа не сможет, ее дальнейшее выполнение не имеет смысла.

2. Множество использовано из-за быстрой проверки наличия элемента и отсутствия в нем дублирующихся элементов

3. Не очень поняла требование "выходим из скрипта с кодом «1»", надеюсь, exit(1) с условием - это то, что запрашивалось.


Как всегда, спасибо за доступно подаваемую информацию на лекциях и информативные ответы в чате!