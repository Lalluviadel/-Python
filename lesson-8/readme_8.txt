ДЗ 08
12.03.21 - 13.03.21

1. Ответ на вопрос задания:
 - имеет ли смысл в данном случае использовать функцию re.compile()?
 Считаю, что в данном случае нет смысла, т.к. мы используем регулярное выражение единожды. Если бы мы использовали его в программе где-либо еще, например, с разными методами (match, search), то было бы лучше иметь уже готовое скомпилированное регулярное выражение.

 Я намеренно результатом функции сделала не сам словарь, а элементы, которые ниже в коде становятся его значениями. Мне кажется, если возвращать именно словарь, то функция будет перегруженной? Но может, я ошибаюсь.

 2. Очень интересное задание.
 Любопытно, что проверка для регулярок regex101.com требует экранировать знаки / в регулярном выражении, а PyCharm - напротив, выдает предупреждение "Redundant character escape '\/' in RegExp". Все, что я смогла найти - упоминания об этом на англоязычных форумах, где советуют оставлять слэши экранированными, так как в этом случае PyCharm иногда может вводить в заблуждение.

 Особенные строки - с IPv6.

 На задание 4 увы, не хватило времени.